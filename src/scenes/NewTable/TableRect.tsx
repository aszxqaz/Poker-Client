export const TableRect = () => {
  // const dimensions = useChakraDimensions({ current: document.documentElement }, true)

  // const screenWidth = dimensions?.marginBox.width
  // const screenHeight = dimensions?.marginBox.height

  // console.log(screenHeight)
  // console.log(screenWidth)

  // const base = screenWidth && screenHeight && Math.max(screenWidth, screenHeight)

  // let isPortrait = false
  // let height, width

  // if (screenHeight && screenWidth)
  //   if (base === screenHeight) {
  //     isPortrait = true
  //     height = 0.8 * screenHeight
  //     width = 0.5 * height
  //     if (width > screenWidth) {
  //       width = screenWidth * 0.95
  //       height = 2 * width
  //     }
  //   } else {
  //     width = 0.8 * screenWidth
  //     height = 0.5 * width
  //     if (height > 0.7 * screenHeight) {
  //       height = 0.7 * screenHeight
  //       width = 2 * height
  //     }
  //   }
}